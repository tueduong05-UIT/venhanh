import 'dart:ui';

class Pallete {
  static const Color primaryColor = Color.fromARGB(255, 255, 220, 20);
  static const Color backgroundColor = Color.fromARGB(255, 255, 255, 255);

  static const Color blackColor = Color.fromARGB(255, 0, 0, 0);
  static const Color lightGrayColor = Color.fromARGB(255, 200, 200, 200);

  static const Color textColor = Color.fromARGB(255, 40, 40, 40);
  static const Color errorColor = Color.fromARGB(255, 190, 0, 35);
}
